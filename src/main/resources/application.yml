spring:
  application:
    name: kameleon-task
  datasource:
    url: jdbc:h2:mem:kameleon-db
    username: username
    password: password
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: never
  h2:
    console:
      enabled: true

